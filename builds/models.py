from django.db import models

# Create your models here.
# class Build(models.Model):
#     name = models.CharField(max_length=100, default=None)
#     processor = models.ForeignKey("components.Component")
